# hello-world

The smallest possible cap2UI5 sample. Demonstrates wiring `@cap2ui5/frontend`
to `@cap2ui5/backend` with no database and no extra config.

## Run

```bash
npm install
npm run dev
```

## Scaffold a fresh copy

```bash
npx create-cap2ui5-app my-app --sample=hello-world
```
